[{関連リンク}]({関連リンク})  | [{関連リンク2}]({関連リンク2})  | ...

{チケットURL、設計ドキュメント、関連PRなどがあればヘッダーとして並べる。なければこの行ごと省く。}

## なぜこの変更をするか

{この PR が解いている問題と、マージ後に何ができるようになるかを、ちょうど1文で書く。}

## 注意してほしいこと

- {レビュアーに伝えるべき警告・マイグレーション・互換性の制約・意図的に対応しなかったこと・驚く判断を1〜3項目。該当がなければ「- なし。」と書く。}

## 変更の輪郭

{下のビューのうち、実装を説明するのに必要な最小の組み合わせだけを使う。既存の形への変更は diff ブロック、大半が新規の形は完成形のブロックで示す。使わないビューの見出しは作らない。}

{...短い説明...}

```diff|sql|json|など
{変更された SQL のテーブル・主要なカラムと関連、エンドポイントのリクエスト/レスポンス契約。}
```

{...短い説明...}

```typescript|python|など
{実装の鍵になる新しいデータ構造や型。}
```

{...短い説明...}

```diff
{変更された挙動の擬似コード。}
```

{...短い説明...}

```diff
{変更された責務が分かる浅いファイルツリー。}
```

{...短い説明...}

```diff
{変更された React のコンポーネントツリー、重要な hooks や state、パッケージ境界。}
```

{...短い説明...}

```diff
{変更されたコールツリー、コールスタック、制御フロー、データフロー。}
```

{一番分かりやすい順に語る。ファイルから見せたほうがよいこともあれば、データ構造・SQL テーブル・API 契約を先に置いたほうがよいこともある。すべてのビューと小見出しは任意で、固定の並びではなくその変更に合った名前と順序を使う。人が人に宛てて書く。既存の形への的を絞った変更には diff を、新規・俯瞰的・diff 記法がかえって読みにくい場合は言語指定または text のブロックで完成形を示す。}
